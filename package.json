{
  "name": "commerce-toolkit",
  "namespace": "@bsmnt",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "examples/*",
    "packages/*",
    "internal/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter=@bsmnt/storefront-hooks --filter=@bsmnt/sdk-gen",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "generate": "turbo run generate",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {},
  "packageManager": "yarn@1.22.19"
}
